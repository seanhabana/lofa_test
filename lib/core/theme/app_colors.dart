import 'package:flutter/material.dart';

class AppColors {
  AppColors._(); // prevents instantiation

  // Brand colors
  static const Color primary = Color(0xFF581C87);
  static const Color secondary = Color(0xFF9D65AA);

  // Backgrounds
  static const Color lightBackground = Color(0xFFF7F7FB);
  static const Color darkBackground = Color(0xFF181C3C);

  // Neutrals
  static const Color white = Colors.white;
  static const Color black = Colors.black;

  // Status colors
  static const Color error = Color(0xFFD32F2F);
  static const Color success = Color(0xFF2E7D32);
}
